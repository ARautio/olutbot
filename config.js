
function getConfigs(){
  const config = {
    botname = "Olutbot",
    project_id = "Olutbot",
    bucket_name = "olutbot",
    telegramtoken = "test"
  }
  return config;
}

module.exports = getConfigs;
